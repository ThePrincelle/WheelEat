import 'package:flutter/material.dart';

class WheelPage extends StatelessWidget {
  const WheelPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Wheel Page'),
    );
  }
}
